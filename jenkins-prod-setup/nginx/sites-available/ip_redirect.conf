server {
    listen 80 default_server;
    listen [::]:80 default_server;
    server_name _;

    # Redirect raw IP or any unmatched host to the canonical domain (preserve path + query)
    return 301 https://jenkins.appleslabs.com$request_uri;
}